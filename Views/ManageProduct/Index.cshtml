@model IEnumerable<Product>
@{
    ViewData["Title"] = "Product Management";
}
<div class="container mt-5">
    <div class="table-wrapper">
        <div class="table-title pb-4">
            <div class="row mt-3">
                <div class="col-sm-6">
                    <h2>Product Management</h2>
                </div>
                <div class="col-sm-6">
                    <a asp-controller="ManageProduct" asp-action="Create" class="btn btn-primary float-end text-uppercase align-items-center">
                        <i class="bi bi-plus-circle pe-2"></i>Create New Product
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-striped table-hover" aria-describedby="table">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>@Html.DisplayNameFor(model => model.CatId)</th>
                    <th>@Html.DisplayNameFor(model => model.Description)</th>
                    <th>@Html.DisplayNameFor(model => model.Origin)</th>
                    <th>@Html.DisplayNameFor(model => model.UnitPrice)</th>
                    <th>Actions</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td>@obj.Name</td>
                        <td>@obj.CatId</td>
                        <td>@obj.Description</td>
                        <td>@obj.Origin</td>
                        <td>@obj.UnitPrice</td>

                        <td>
                            <a class="edit" asp-action="Edit" asp-route-id="@obj.ProdID"><i class="bi bi-pen-fill"></i></a>
                            <a class="delete" asp-action="Delete" asp-route-id="@obj.ProdID"><i class="bi bi-trash3-fill"></i></a>
                        </td>
                    </tr>
                }    
                </tbody>
        </table>
    </div>
</div>
